<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <nav>
        <div class="mt-3">
          <div>
            <%= form_with(url: search_users_path, method: :get, data: {turbo_frame: "name_search"}) do |form| %>
              <%= form.text_field :query, oninput:"this.form.requestSubmit()", class: "form-control" %>
            <% end %>
            <div id="name_search"></div>           
          </div>
          <div>
            <% if current_user %>
              <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-primary rounded-1" %>
            <% else %>
              <%= link_to 'Sign in', user_session_path %>
            <% end %>
          </div>
        </div>
        <% if notice.present? %>
          <div class="notice">
            <p class="bg-warning text-center text-white"> <%= notice %> </p>
          </div>
        <% end %>
        <% if alert.present? %>
          <div class="alert">
            <p class="bg-danger text-center text-white"> <%= alert %> </p>
          </div>
        <% end %>
      </nav>
    </div>
  </div>
</nav>